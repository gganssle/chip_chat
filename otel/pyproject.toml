[project]
name = "chip-chat-otel"
version = "0.0.0"
description = "Shared OpenInference/OpenTelemetry instrumentation for Chip Chat."
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/chip_chat"]
